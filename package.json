{
  "name": "high-performance-gis-topology",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "dev:backend": "cd backend && ./mvnw spring-boot:run",
    "dev:full": "npm run dev & npm run dev:backend",
    "build": "cd frontend && npm run build",
    "build:backend": "cd backend && ./mvnw package -DskipTests",
    "test": "cd frontend && npm run test:unit",
    "test:backend": "cd backend && ./mvnw test"
  }
}
